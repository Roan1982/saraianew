# Variables de entorno para SARA Asistente Virtual con Docker

# Configuración de X11 (para interfaz gráfica)
# En Linux: normalmente :0 o :1
# En Windows con WSL2: configurar VcXsrv y usar la IP de WSL
DISPLAY=:0

# URL del backend (normalmente no cambiar)
API_URL=http://backend:8000/api

# Configuración de Django (opcional)
DJANGO_SETTINGS_MODULE=sara.settings
DEBUG=True

# Base de datos (SQLite por defecto, persistente en contenedor)
# DATABASE_URL=sqlite:///app/db.sqlite3